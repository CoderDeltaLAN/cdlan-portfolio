<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="icon" href={import.meta.env.BASE_URL}favicon.svg type="image/svg+xml">
  <meta name="theme-color" content="#0a0a1a">
  <meta property="og:title" content="CoderDeltaLAN — Portfolio">
  <meta property="og:description" content="OSS Projects · Python · DevOps · AI Tools">
  <meta property="og:type" content="website">
  <meta property="og:url" content="https://coderdeltalan.github.io/cdlan-portfolio/">
  <meta property="og:image" content="https://coderdeltalan.github.io/cdlan-portfolio/assets/placeholder.svg">

  <meta charset="UTF-8" />
  <base href={import.meta.env.BASE_URL}>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <meta name="description" content="CoderDeltaLAN — Open Source Portfolio · Python · DevOps · AI Tools"/>
  <title>CoderDeltaLAN — Portfolio</title>

  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;500;700;800&display=swap">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;500;700;800&display=swap">

  <style is:global>
    :root{
      --bg:#0a0a1a; --text:#e7e9f0; --muted:#a8b0c2;
      --accent:#4d8af0; --accent-2:#7aa9ff; --particle:#4d8af0;
      --card:#151b30; --radius:16px;
    }
    *{margin:0;padding:0;box-sizing:border-box}
    body{
      font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,sans-serif;
      color:var(--text);
      background: radial-gradient(1200px 800px at 50% -200px,#0f1530 0%,var(--bg) 40%) fixed;
      overflow-x:hidden;
    }
    .frame{position:fixed; inset:18px; pointer-events:none; z-index:2;
      border:1px solid rgba(255,255,255,.08); border-radius:22px;
      box-shadow:inset 0 0 0 1px rgba(77,138,240,.08);}
    #bg-net{position:fixed; inset:0; z-index:-1;}
    .wrap{max-width:1100px; margin:0 auto; padding:100px 22px 70px;}

    /* HERO */
    .hero{text-align:center; margin-bottom:40px;}
    .title{
      font-weight:800; font-size:clamp(32px,6vw,60px);
      background:linear-gradient(90deg,#7aa9ff 0%,#ffffff 50%,#4d8af0 100%);
      -webkit-background-clip:text; background-clip:text; color:transparent;
      animation:glowTitle 4s ease-in-out infinite alternate;
      position:relative;
    }
    @keyframes glowTitle { from { text-shadow:0 0 8px #4d8af0; } to { text-shadow:0 0 20px #7aa9ff; } }
    .title::after{
      content:""; position:absolute; inset:-10px;
      background:linear-gradient(120deg,transparent 40%,rgba(255,255,255,.10) 50%,transparent 60%);
      filter:blur(6px); mix-blend-mode:screen; opacity:0; transform:translateX(-28%);
      transition:opacity .2s ease;
    }
    .title:hover::after{ animation:sweep .8s ease; opacity:1; }
    @keyframes sweep{from{transform:translateX(-28%)}to{transform:translateX(28%)}}

    .subtitle{color:var(--muted); margin-top:12px; font-size:18px;}
    .top-actions{margin:30px auto; display:flex; gap:14px; justify-content:center; flex-wrap:wrap;}
    .btn{
      display:inline-flex; align-items:center; gap:10px;
      padding:12px 22px; border-radius:999px;
      border:1px solid rgba(77,138,240,.45);
      background:linear-gradient(180deg, rgba(77,138,240,.12), rgba(77,138,240,.04));
      color:var(--text); text-decoration:none; font-weight:600;
      transition:.28s ease; position:relative; overflow:hidden;
    }
    .btn:hover{transform:translateY(-2px); border-color:var(--accent-2);
      box-shadow:0 0 18px rgba(77,138,240,.6);}
    .btn::before{content:""; position:absolute; inset:0; left:-100%;
      background:linear-gradient(90deg,transparent,rgba(255,255,255,.35),transparent);
      transition:.6s;}
    .btn:hover::before{left:100%}

    /* PROJECTS PANEL with neon border */
    .projects{
      margin-top:50px; position:relative; border-radius:var(--radius); padding:24px;
      background:linear-gradient(180deg, rgba(255,255,255,.04), rgba(255,255,255,.02));
      border:1px solid rgba(255,255,255,.08);
      box-shadow:0 10px 40px rgba(0,0,0,.35), inset 0 0 0 1px rgba(255,255,255,.04);
      overflow:hidden;
    }
    .projects::before{
      content:""; position:absolute; inset:-2px; border-radius:calc(var(--radius) + 2px);
      background:
        conic-gradient(from 180deg at 50% 50%, rgba(122,169,255,.0), rgba(122,169,255,.35), rgba(77,138,240,.45), rgba(122,169,255,.0));
      filter:blur(8px); opacity:.65;
      mask:linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
      -webkit-mask:linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
      padding:2px; box-shadow:0 0 40px rgba(77,138,240,.25);
      animation:spin 12s linear infinite;
    }
    @keyframes spin{to{transform:rotate(360deg)}}

    .projects h2{
      font-size:24px; margin-bottom:6px; color:#d9e6ff;
      display:flex; align-items:center; gap:10px; position:relative; z-index:1;
    }
    .projects svg{width:22px; height:22px; fill:var(--accent);}
    .projects p{margin-bottom:18px; color:#a8b0c2; position:relative; z-index:1;}
    .cards{display:grid; gap:16px; grid-template-columns:repeat(2, minmax(0,1fr)); position:relative; z-index:1;}
    @media (max-width:560px){ .cards{ grid-template-columns:1fr; } }

    /* anchor-cards */
    .card, .card:link, .card:visited, .card:hover, .card:active{
      color:var(--text); text-decoration:none;
    }
    .card{
      background:var(--card);
      border:1px solid rgba(255,255,255,.08);
      border-radius:14px; padding:16px;
      transition:.25s ease; cursor:pointer;
    }
    .card:hover{
      transform:translateY(-4px);
      border-color:rgba(77,138,240,.6);
      box-shadow:0 0 20px rgba(77,138,240,.5), 0 0 30px rgba(77,138,240,.3);
    }
    .p-head{display:flex; align-items:center; justify-content:space-between; margin-bottom:6px;}
    .p-title{display:flex; align-items:center; gap:8px; font-weight:700; color:#dbe6ff;}
    .p-title svg{width:16px;height:16px; fill:#9ec0ff; opacity:.85;}
    .badge{font-size:12px; border:1px solid rgba(255,255,255,.12); padding:2px 8px; border-radius:999px; color:#cfe0ff}
    .v{font-size:15px; color:#eaf1ff; line-height:1.5}

    /* Footer */
    footer{text-align:center; color:#a8b0c2; margin:42px 0 8px; position:relative;}
    footer::before{
      content:""; position:absolute; left:50%; transform:translateX(-50%);
      top:-16px; width:220px; height:1px;
      background:linear-gradient(90deg,transparent, rgba(122,169,255,.7), transparent);
      box-shadow:0 0 14px rgba(77,138,240,.55);
    }
    .phrase{ color:#cfe0ff; text-shadow:0 0 10px rgba(77,138,240,.45); margin:6px 0 6px; }
    .license{
      color:#b8c4e6; font-size:14px;
      background:linear-gradient(180deg, rgba(122,169,255,.08), rgba(122,169,255,.02));
      display:inline-block; padding:6px 10px; border-radius:999px;
      border:1px solid rgba(122,169,255,.25);
      box-shadow:0 0 14px rgba(77,138,240,.25) inset, 0 0 10px rgba(77,138,240,.15);
    }
  </style>
</head>
<body>
  <canvas id="bg-net"></canvas>
  <div class="frame"></div>
  <div class="wrap">
    <!-- HERO -->
    <section class="hero">
      <h1 class="title">CoderDeltaLAN</h1>
      <p class="subtitle">OSS Projects · Python · DevOps · AI Tools</p>
      <div class="top-actions">
        <a class="btn" href="https://github.com/CoderDeltaLAN" target="_blank" rel="noopener">GitHub</a>
        <button class="btn" id="btn-email" type="button">Email</button>
        <a class="btn" href="https://github.com/CoderDeltaLAN?tab=repositories&type=public" target="_blank" rel="noopener">Repos</a>
      </div>
    </section>

    <!-- PROJECTS -->
    <section class="projects">
      <h2>
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M12 2l4 4-4 4-4-4zM2 12l4-4 4 4-4 4zM12 22l-4-4 4-4 4 4zM22 12l-4 4-4-4 4-4z"/></svg>
        Featured Projects
      </h2>
      <p>Maintained and developed by <strong>Yosvel — CoderDeltaLAN</strong>. High-quality open source under the <strong>Always Green</strong> policy.</p>

      <div class="cards">
        <a class="card" href="https://github.com/CoderDeltaLAN/pybuglint" target="_blank" rel="noopener">
          <div class="p-head">
            <div class="p-title">
              <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M12 2l4 2-2 4 4 2-2 4 4 2-8 6-8-6 4-2-2-4 4-2-2-4 4-2z"/></svg>
              pybuglint
            </div>
            <span class="badge">Python · Lint</span>
          </div>
          <div class="v">Static analysis to catch bugs early. Strict pipeline: ruff, black, pytest, CI/CD.</div>
        </a>

        <a class="card" href="https://github.com/CoderDeltaLAN/ai-patch-verifier" target="_blank" rel="noopener">
          <div class="p-head">
            <div class="p-title">
              <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M3 6h18v2H3zM3 11h10v2H3zM3 16h14v2H3z"/></svg>
              ai-patch-verifier
            </div>
            <span class="badge">Python · CLI</span>
          </div>
          <div class="v">Verify AI-generated patches with linting, tests and security checks. Automated CI reports.</div>
        </a>
      </div>
    </section>

    <footer>
      <div class="phrase">"It works on my machine 🤷"</div>
      <div class="license">License: MIT · © 2025 CoderDeltaLAN</div>
    </footer>
  </div>

  <script>
    // @ts-nocheck

    /* Constellation canvas */
    (function(){
      const canvasEl = document.getElementById('bg-net');
      if (!canvasEl || !canvasEl.getContext) return;
      const ctx = canvasEl.getContext('2d', {alpha:true});
      if (!ctx) return;

      const DPR = Math.min(2, window.devicePixelRatio || 1);
      let points = [];
      let W = 0, H = 0;

      function rs(){
        const w = innerWidth, h = innerHeight;
        canvasEl.style.width = w + 'px';
        canvasEl.style.height = h + 'px';
        canvasEl.width  = Math.floor(w * DPR);
        canvasEl.height = Math.floor(h * DPR);
        ctx.setTransform(DPR,0,0,DPR,0,0);
        W = w; H = h; init();
      }
      addEventListener('resize', rs, {passive:true}); rs();

      function init(){
        points.length = 0;
        const n = Math.max(48, Math.floor((W*H)/24000));
        for(let i=0;i<n;i++){
          points.push({
            x: Math.random()*W, y: Math.random()*H,
            vx: (Math.random()*1.2-0.6), vy: (Math.random()*1.2-0.6),
            r: Math.random()*1.8+0.6
          });
        }
      }

      function hex2rgb(hex){
        hex = String(hex).replace('#','');
        if(hex.length===3) hex = hex.split('').map(c=>c+c).join('');
        const n = parseInt(hex,16);
        return [(n>>16)&255,(n>>8)&255,n&255];
      }

      function tick(){
        const [r,g,b] = hex2rgb(getComputedStyle(document.documentElement).getPropertyValue('--particle').trim()||'#4d8af0');
        const maxD = Math.min(180, Math.max(110, Math.hypot(W,H)/14));
        ctx.clearRect(0,0,W,H);

        for (const p of points){
          p.x+=p.vx; p.y+=p.vy;
          if(p.x<0||p.x>W) p.vx*=-1;
          if(p.y<0||p.y>H) p.vy*=-1;
          ctx.beginPath();
          ctx.arc(p.x,p.y,p.r,0,Math.PI*2);
          ctx.fillStyle = `rgba(${r},${g},${b},0.75)`;
          ctx.fill();
        }
        for (let i=0;i<points.length;i++){
          for (let j=i+1;j<points.length;j++){
            const dx=points[i].x-points[j].x, dy=points[i].y-points[j].y;
            const d2=dx*dx+dy*dy;
            if(d2<maxD*maxD){
              const a=1-Math.sqrt(d2)/maxD;
              ctx.strokeStyle=`rgba(${r},${g},${b},${0.20*a})`;
              ctx.lineWidth=1;
              ctx.beginPath();
              ctx.moveTo(points[i].x,points[i].y);
              ctx.lineTo(points[j].x,points[j].y);
              ctx.stroke();
            }
          }
        }
        requestAnimationFrame(tick);
      }
      tick();
    })();

    /* Email button: copy + centered toast */
    (function(){
      const EMAIL="coderdeltalan.cargo784@8alias.com";
      const btn=document.getElementById('btn-email');

      function toast(msg){
        let host=document.getElementById('toast-host');
        if(!host){
          host=document.createElement('div');
          host.id='toast-host';
          host.style.position='fixed';
          host.style.top='18px';
          host.style.left='50%';
          host.style.transform='translateX(-50%)';
          host.style.zIndex='9999';
          document.body.appendChild(host);
        }
        const el=document.createElement('div');
        el.textContent=msg;
        el.style.padding='10px 14px';
        el.style.marginTop='8px';
        el.style.borderRadius='12px';
        el.style.background='rgba(13,20,36,0.95)';
        el.style.color='#e7e9f0';
        el.style.border='1px solid rgba(77,138,240,.6)';
        el.style.boxShadow='0 6px 26px rgba(0,0,0,.45), 0 0 18px rgba(77,138,240,.35)';
        el.style.fontSize='14px';
        el.style.backdropFilter='blur(6px)';
        host.appendChild(el);
        setTimeout(()=>{el.style.transition='opacity 300ms ease'; el.style.opacity='0'; setTimeout(()=>el.remove(),320);},1600);
      }

      async function copyEmail(){
        try{
          await navigator.clipboard.writeText(EMAIL);
          toast('Email copied to clipboard');
        }catch{
          const ta=document.createElement('textarea'); ta.value=EMAIL; document.body.appendChild(ta);
          ta.select(); document.execCommand('copy'); ta.remove(); toast('Email copied to clipboard');
        }
      }
      btn?.addEventListener('click', copyEmail);
    })();

    /* Tilt cards */
    (function(){
      const cards = document.querySelectorAll('.card');
      cards.forEach((card)=>{
        let rAF = 0;
        card.addEventListener('mousemove',(e)=>{
          cancelAnimationFrame(rAF);
          rAF=requestAnimationFrame(()=>{
            const me = e;
            const r = card.getBoundingClientRect();
            const x=(me.clientX-r.left)/r.width-0.5;
            const y=(me.clientY-r.top)/r.height-0.5;
            card.style.transform=`translateY(-4px) rotateX(${(-y*4).toFixed(2)}deg) rotateY(${(x*4).toFixed(2)}deg)`;
          });
        });
        ['mouseleave','blur'].forEach((ev)=>card.addEventListener(ev,()=>{ card.style.transform=''; }));
      });
    })();
  </script>
</body>
</html>
