---
const { title, desc, href, tags=[], image='/assets/placeholder.svg' } = Astro.props;
---
<article class="glass p-5 hover:scale-[1.01] transition">
  <img src={image} alt={title} class="rounded-lg w-full h-44 object-cover mb-4" loading="lazy" />
  <h3 class="text-lg font-semibold">{title}</h3>
  <p class="text-[15px] text-[var(--muted)] mt-1">{desc}</p>
  <div class="mt-3 flex gap-2 flex-wrap">{tags.map(t => <span class="text-[12px] px-2 py-1 rounded border/40 border">{t}</span>)}</div>
  <a href={href} class="inline-block mt-4 text-sm underline">Abrir proyecto →</a>
</article>
